"use strict"; //treats all the JS code as newer version of JavaScript
// alert("3+3"); using nodejs not console 
let name = "Muwahid Cheema";
let age = 20;
let isLoggedIn = true;

//number bigint string boolean null(stand alone value) undefined symbol(uniqueness) 
// object(array,object literal,function,date,anything else)

console.log(typeof null);